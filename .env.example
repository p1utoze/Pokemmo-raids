# Database paths
# DATA_PATH=data/bosses.json
# CHECKLIST_DB=data/checklist_xmas.db
# ADMIN_DB=data/admin.db

# Admin authentication
ADMIN_PASSWORD=your-secure-admin-password
ADMIN_SECRET=your-jwt-secret-key

# MongoDB Configuration
MONGO_INITDB_USERNAME=your-mongo-username
MONGO_INITDB_PASSWORD=your-secure-mongo-password
MONGO_DB
# SMTP Configuration for password reset emails

# Option 1: Python debugging SMTP server (for local development/testing)
# Run in a separate terminal: python3 -m smtpd -n -c DebuggingServer localhost:1025
# This will print emails to console instead of sending them
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test@local
SMTP_PASSWORD=test
SMTP_FROM=noreply@pokemmoraids.local

# Option 2: Gmail (for production)
# For Gmail: Use app-specific password (https://support.google.com/accounts/answer/185833), Refer: https://myaccount.google.com/apppasswords
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@pokemmoraids.com

# Option 3: Other providers
# For other providers: Check their SMTP settings
# Common ports: 587 (TLS), 465 (SSL), 25 (plain)

# Server configuration
# PORT=8080
